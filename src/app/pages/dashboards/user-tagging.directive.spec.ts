import { UserTaggingDirective } from './user-tagging.directive';

describe('UserTaggingDirective', () => {
  it('should create an instance', () => {
    const directive = new UserTaggingDirective();
    expect(directive).toBeTruthy();
  });
});
